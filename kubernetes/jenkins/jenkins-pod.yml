apiVersion: v1
kind: Pod
metadata:
  name: jenkins-pod
spec:
  containers:
    - name: jenkins
      image: jenkins:2.60.3-alpine
      ports:
        - containerPort: 8080
        - containerPort: 50000
