apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-persistentvolumeclaim
  labels:
    app: mongodb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi