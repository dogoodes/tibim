apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: nexus-ingress
  namespace: nexus
  labels:
    app: nexus
spec:
  securityContext:
    fsGroup: 200
  backend:
    serviceName: nexus-service
    servicePort: 8081
