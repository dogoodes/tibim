apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexus-persistentvolumeclaim
  namespace: nexus
  labels:
    app: nexus
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 3Gi
