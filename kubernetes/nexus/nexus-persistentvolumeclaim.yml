apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexus3-persistentvolumeclaim
  namespace: nexus3
  labels:
    app: nexus3
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
