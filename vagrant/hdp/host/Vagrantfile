# -*- mode: ruby -*-
# vi: set ft=ruby :

vagrantfiles = %w[master/Vagrantfile worker/Vagrantfile]
vagrantfiles.each do |vagrantfile|
  load File.expand_path(vagrantfile) if File.exists?(vagrantfile)
end